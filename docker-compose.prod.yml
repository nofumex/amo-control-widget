services:
  api:
    restart: unless-stopped
    environment:
      APP_ENV: production
  worker:
    restart: unless-stopped
    environment:
      APP_ENV: production
  postgres:
    restart: unless-stopped
  redis:
    restart: unless-stopped
